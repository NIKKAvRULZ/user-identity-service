spring:
  application:
    name: user-identity-service

# Ensure 'springdoc' is at the top level, same as 'spring'
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: alpha
  writer-with-default-pretty-printer: true